/**
 * supabase.ts — DISABLED / DELETED
 *
 * NO databases. NO server proxies. NO Supabase. NO edge functions.
 * 
 * Everything is 100% static / client-side:
 *   - Config: fetched from https://raw.githubusercontent.com/cebacoco/configs/main/cebacoco-config.ini
 *   - Sync:   fetched from https://raw.githubusercontent.com/cebacoco/configs/main/sync.ini
 *   - Email:  sent directly via SMTP2GO API key
 *
 * This file exists only so imports don't break.
 */

export const supabase = null;
